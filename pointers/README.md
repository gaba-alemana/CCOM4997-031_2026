# Practicando punteros

## Clase para crear arreglos dinámicos de enteros 
#### Atributos
```
protected:
  
  int *arr = NULL ; 
  int used = 0 ; 
  int length = 0 ; 

```

#### Métodos
```
public:
  void set_length (int) ;
  void insert (int) ;
  void resize (int) ;
  void display () const ;
  int* max () const ;
  void swap () ;
  IntArray overload= (IntArray) ;

  
```
